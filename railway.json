{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "php artisan storage:link && php artisan migrate --seed --force && service nginx start && php-fpm",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}
